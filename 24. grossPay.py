#4. Write a program to prompt the user for hours and rate per hour
#to compute gross pay.

hours = float(input("Enter the hours "))
ratePerHour = float(input("Enter the rate per hour "))
print("gross pay "+str(hours*ratePerHour))
